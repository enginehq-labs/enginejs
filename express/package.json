{
  "name": "@enginehq/express",
  "version": "0.1.2",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@enginehq/core": "0.1.2",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "node -e \"const fs = require('node:fs'); fs.rmSync('dist-test',{recursive:true,force:true});\" && npm -C .. --workspace @enginehq/core run build && tsc -p tsconfig.test.json && find dist-test/test/unit -name '*.test.js' | xargs node --test",
    "test:integration": "node -e \"const fs = require('node:fs'); fs.rmSync('dist-test',{recursive:true,force:true});\" && npm -C .. --workspace @enginehq/core run build && tsc -p tsconfig.test.json && find dist-test/test/integration -name '*.test.js' | xargs node --test",
    "test": "npm run test:unit && npm run test:integration"
  },
  "publishConfig": {
    "access": "public"
  }
}
