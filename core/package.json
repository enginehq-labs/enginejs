{
  "name": "@enginehq/core",
  "version": "0.1.1",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "pg": "^8.16.3",
    "sequelize": "^6.37.7"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "tsc -p tsconfig.test.json && node --test dist-test/test/unit/*.test.js",
    "test:integration": "tsc -p tsconfig.test.json && node --test dist-test/test/integration/*.test.js",
    "test": "npm run test:unit && npm run test:integration"
  },
  "publishConfig": {
    "access": "public"
  }
}
