@AGENTS.md

# CLAUDE.md

Instructions for Claude Code in this repository.

- `AGENTS.md`, imported on the first line, holds the project facts and rules for every AI tool.
- `.claude/rules/communication.md` holds the communication rules. Claude Code loads it in every session.
- `.claude/agents/` holds the project subagents: `verifier`, `doc-auditor`, `ste-reviewer` and `diff-reviewer`.
